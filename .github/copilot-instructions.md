# Copilot Repository Instructions

Before every task, read and follow the root `AGENTS.md` and `AI-AGENT-OPERATING-CONTRACT.md`.

The repository rules are authoritative. Inspect before modifying, preserve existing work, avoid mocks/placeholders/hard-coded runtime state, protect secrets and trading/model safeguards, make targeted changes, and perform concrete verification before reporting completion.