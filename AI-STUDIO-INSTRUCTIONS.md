# AI Studio Repository Instructions

At the beginning of every task, read the root `AGENTS.md` and `AI-AGENT-OPERATING-CONTRACT.md` before inspecting or modifying code.

Treat `AGENTS.md` as the primary project authority and the operating contract as the persistent AI-agent enforcement layer.

Do not bypass repository rules. Inspect before modifying; preserve existing work; use dynamic, backend-authoritative implementations; never fabricate data, model state, broker capabilities, or trading outcomes; protect secrets; keep changes targeted; and perform concrete verification before declaring work complete.

If the requested change conflicts with repository rules or lacks sufficient context, stop and inspect further or ask for clarification instead of implementing a shortcut.